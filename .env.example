# Optional: Fallback Anthropic API key for AI Image to BIM feature
# Users provide their own key via the UI (BYOK pattern)
# This is only used as a fallback when no user key is provided
# Get your key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
